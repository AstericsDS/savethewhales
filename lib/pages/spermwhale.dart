import 'package:flutter/material.dart';

class SpermWhale extends StatelessWidget {
  const SpermWhale({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Paus Sperma'),
        ),
        body: main());
  }

  Widget main() {
    return Column();
  }
}
