import 'package:flutter/material.dart';

class BlueWhale extends StatelessWidget {
  const BlueWhale({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Paus Biru'),
        ),
        body: main());
  }

  Widget main() {
    return Column();
  }
}
